// export function attachListener(event: string, elementId: string, callback): void {
//   const element = document.getElementById(elementId);
//   element?.addEventListener(event, this.eventHandlerAuthorSelected.bind(this)); // event listener

// }
