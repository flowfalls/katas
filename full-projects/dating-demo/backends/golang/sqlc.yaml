version: 1
packages:
  - path: "repositories"
    name: "repositories"
    engine: "postgresql"
    schema: "schema.sql"
    queries: "query.sql"
    emit_prepared_queries: true
    emit_interface: false
    emit_exact_table_names: false
    emit_json_tags: true
